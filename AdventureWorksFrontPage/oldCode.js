    //var img = document.getElementsByClassName("img1")
    //for (i = 0; i < img.length; i++) {
    //  fade(img[i])
    //}

//function swapImage()

//function mainMenu() {
 //   var imgs = document.getElementsByTagName("img")
  ////  for (i = 0; i < imgs.length; i++) {
      //  fade(imgs[i], swapImage)
  //  }
////}


//function swapImage(e) {
    //fade(e)
      //  var oldSrc = e.src
      //  var num = oldSrc.charAt(oldSrc.length - 5)
       // var index = oldSrc.lastIndexOf(num)
       // if (num < 4) {
         //   num = parseInt(num) + 1
        //} else { num = 1 }
        //var newSrc = oldSrc.substring(0, index) + num + oldSrc.substring(index + 1, oldSrc.length)
       //// e.src = newSrc
//}

// the swap image function is passed to the fade function.  is only run once check flag is false
//function fade(e, callback) {
    //var increment = +0.1
    //var op = 1
    //var check = true

    //var timer = setInterval(function () {
        //op = op - increment
        //e.style.opacity = op
        //if (op <= 0.3) {
            //check = false
            //clearInterval(timer)
       // }
    //}, 50)

    //var checking = setInterval(function () {
       // if (!check) {
            //callback(e)
            //clearInterval(checking)
        //};
    //}, 50)

    //}